import User from '../models/User';

// TODO: setup routing and add POST routes for user
/*  User.create({ username: 'Strategy', rank: 1 }, (err: Error, newUser: any) => {
  if (err) {
    console.log(err);
  } else {
    console.log(newUser);
  }
});  */